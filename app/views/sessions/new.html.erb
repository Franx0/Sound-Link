<h1>CREATE YOUR OWN SESSION</h1>

<% if flash[:error] %>
  <%= flash[:error] %>
<% end %>

<% if @session.errors.any? %>
  <ul>
    <% @session.errors.full_messages.each do |error_message| %>
    <li> <%= error_message %> </li>
    <% end %> 
  </ul>
<% end %>

<section id='session'>  
  <%= form_for [@user, @session] do |f| %>
    <div class="field">
      <%= f.label :session_name %></br>
      <%= f.text_field :session_name %></br>
      <%= f.label :description %></br>
      <%= f.text_area(:description, size:'50x10') %></br>
    </div>
    <div class="actions">
     <%= f.submit 'Create', :class => 'button' %>
    </div>
  <% end %>
  <%= link_to "Cancel", user_sessions_path(@user), :class => 'trackname' %>
</section>